<% provide :page_title, t('.form.questions') %>

<div class="column-3-4">
  <div class="user-questions buffer-both">
    <% @questions.includes([:hashtags]).each do |question| %>
      <%= link_to "@#{question.user.username}", user_path(question.user) %>
      <%= render partial: 'questions/question', object: question %>
    <% end %>
  </div>
</div>

<%# <div class="buffer-both"></div>
  <div class="questions-list buffer-both">
    <%= render @questions.includes([:hashtags]) %>
  <%# <div> %>
<%# </div> %>